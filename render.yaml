services:
  - type: web
    name: regresion
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python entrenar_modelo.py"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120 --preload"
    autoDeploy: true
